import { SearchCitiesPipe } from './search-cities.pipe';

describe('SearchCitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
