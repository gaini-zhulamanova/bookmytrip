import { ShowInclusivePipe } from './show-inclusive.pipe';

describe('ShowInclusivePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowInclusivePipe();
    expect(pipe).toBeTruthy();
  });
});
