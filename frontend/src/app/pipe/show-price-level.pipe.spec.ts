import { ShowPriceLevelPipe } from './show-price-level.pipe';

describe('ShowPriceLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowPriceLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
